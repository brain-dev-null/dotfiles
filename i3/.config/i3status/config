general {
        colors = true
        interval = 1
}

order += "wireless _first_"
order += "disk /"
order += "battery all"
order += "tztime local"

wireless _first_ {
	format_up = "󰤨 "
	format_down = "󰤯 "
}

tztime local {
	format = "%Y-%m-%d %H:%M:%S"
}

disk "/" {
        format = "%avail"
}

battery all {
        format = "%status %percentage %remaining"
        status_chr = "󱐋"
        status_bat = "󰁾"
        status_full = "󰁹 "
}
